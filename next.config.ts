import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ["pixlr.com", "miro.medium.com"], // add any other domains you need
  },
};

export default nextConfig;
